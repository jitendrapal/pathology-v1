# Additional requirements for PostgreSQL deployments
# Use this file when deploying to platforms with PostgreSQL (DigitalOcean, Heroku, etc.)
# Install with: pip install -r requirements.txt -r requirements-postgres.txt

# PostgreSQL adapter
psycopg2-binary==2.9.9

